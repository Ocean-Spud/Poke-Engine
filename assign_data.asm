; Sorts all user created data to be compiled into "Vulkan readible code"
; Written in assembly due to its reliability with repetitive tasks which need to be done fast (I'm sorry contributers)

section .bss
    ;vars

section.data
    ;consts

section .text
    global _start

    _start: